{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "lastfm",
  "description": "Last.fm / Stats.fm linkage for Twitch users. Used by !song and friends.",
  "bsonType": "object",
  "required": ["_id", "twitch_uid"],
  "properties": {
    "_id": { "bsonType": "objectId" },
    "twitch_uid": {
      "bsonType": "string",
      "description": "Twitch user id of the chatter"
    },
    "lastfm_user": {
      "bsonType": ["string", "null"],
      "description": "Last.fm username, or null when only Stats.fm is set"
    },
    "statsfm_user": {
      "bsonType": ["string", "null"],
      "description": "Stats.fm username, or null when not linked"
    },
    "use_statsfm": {
      "bsonType": "bool",
      "description": "When true, prefer Stats.fm over Last.fm for lookups"
    }
  }
}
