{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "letterboxd",
  "description": "Letterboxd account linkage for Twitch users.",
  "bsonType": "object",
  "required": ["_id", "twitch_uid", "letterboxd_user"],
  "properties": {
    "_id": { "bsonType": "objectId" },
    "twitch_uid": {
      "bsonType": "string",
      "description": "Twitch user id of the chatter"
    },
    "letterboxd_user": {
      "bsonType": "string",
      "description": "Letterboxd username (the path segment after letterboxd.com/)"
    }
  }
}
